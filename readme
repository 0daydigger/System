一、我的感想
1.注意！读取超过1内存页的东西【铁定会崩，一定会崩，100%会崩，是因为kmalloc的原因……我也没有什么好办法……】
2. read函数和fread是不一样的，因此在编写测试文件的时候必须要调用read而不是fread，因为fread操作的是一个文件流，每次要求read的都是块大小。至于缓冲区怎么填充就不知道了，因此会导致一些问题，严重的可以导致内核崩溃（原因就是字符串越节），而用read操作的是open函数带来的文件句柄，因此没有这个问题。
